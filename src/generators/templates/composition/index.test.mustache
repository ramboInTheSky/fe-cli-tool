import React from 'react';
import { shallow } from 'enzyme';

import { {{ name }} } from './';

describe('<{{ name }} />', () => {
  let props;

  beforeEach(() => {
    props = {};
  });

  afterEach(() => {
    jest.clearAllMocks();
  });

  it('should render correctly', () => {
    const mounted = shallow(<{{ name }} {...props} />);
    expect(mounted).toMatchSnapshot();
  });
});
